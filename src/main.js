/**
 * Created by sanchez 
 */
"use strict";
//import "handlebars";
import 'bootstrap/scss/bootstrap.scss';
import 'bootstrap';
import fontawesome from '@fortawesome/fontawesome';
import $ from "jquery";
// import CSS
import css from './assets/css/css.css';
import scss from "./assets/scss/style.scss";
import "./assets/js/index";

/*$(document).ready(function() {
  var defaultSource = $("#default").html();
  var indexSource = $("#index").html();
  Handlebars.registerPartial("default", defaultSource);
  Handlebars.registerHelper("include", function(source) {
    return new Handlebars.SafeString(source);
  });
  var template = Handlebars.compile(indexSource);
  var context = {
    content: "<b>This is some other content</b>",
    header: "<h1>This is a header</h1>",
    footer: "<div class='footer'>This is a footer</div>"
  };
  var html = template(context);
  $("#resultPlaceholder").html(html);
});*/

// import Js Plugins/Entities

